version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
            - pwd
            - ls -lah
            - make up
      prologue:
        commands:
          - git clone git@github.com:revagomes/drupal-pece.git
      epilogue:
        on_pass:
          commands:
            - echo 'Takalepau marko wèi!'
        on_fail:
          commands:
            - 'echo "Nooop :("'
