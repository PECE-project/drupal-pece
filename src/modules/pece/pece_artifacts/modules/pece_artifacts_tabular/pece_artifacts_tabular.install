<?php

/**
 * @file
 * Install, update and uninstall functions for the pece_artifacts_tabular module.
 */

function pece_artifacts_tabular_install () {
  $anon_perms = array(
    'download any document files',
  );
  user_role_grant_permissions(DRUPAL_ANONYMOUS_RID, $anon_perms);
 }
